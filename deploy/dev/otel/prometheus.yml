# Prometheus reference configuration: scrapes the OpenTelemetry Collector's
# Prometheus exporter (which aggregates the agentos process metrics).
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: agentos-otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
