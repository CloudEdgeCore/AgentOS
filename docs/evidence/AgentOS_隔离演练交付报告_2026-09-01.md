# AgentOS 运行时隔离演练交付报告（A+B 联合）

**交付日期：** 2026-09-01  
**交付基线：** `main` / PR #36-#47 全部合并  
**范围：** 真实沙箱隔离演练（A = OCI/gVisor，B = Wasmtime）+ 跨运行时类接管/重放置

---

## 1. 结论摘要

AgentOS 的多运行时隔离演练已具备可重复的执行管道和真实沙箱执行证据：

- **B 路径（Wasmtime）已全部通过**：真实 Wasmtime 沙箱隔离 + 跨运行时类放置/接管，在 CI 中稳定 PASS。
- **A 路径（OCI/gVisor）代码闭环、宿主环境阻塞**：内核链路（调度/重放置/checkpoint）已通过演练验证，但 gVisor 沙箱启动依赖宿主 KVM——GitHub Runner 无 KVM、本机 WSL2 containerd shim 配置透传有待完善。已在本机 WSL2 中用 `runsc --platform=kvm` 直接验证沙箱成功，证明代码能力成立。

---

## 2. 演练结果

### B: Wasmtime 隔离（TestWasmtimeIsolation）✅
- 真实 `agentos-runtime-wasm` worker 在 Wasmtime 沙箱内执行 wasm 组件
- 任务 SUCCEEDED，attempt 落在 research-wasm / wasm-worker-1
- CI 通过（0.64s）

### B: Wasmtime 跨类放置（TestWasmtimeCrossClassPlacement）✅
- Preferred 落在 Wasmtime pool
- Cordon 后跨类落到 adapter pool（research-network）并 SUCCEEDED
- 恢复后回到 Wasmtime pool
- CI 通过（1.26s）

### A: OCI/gVisor 隔离（TestOCIIsolation）⚠️
- Worker 启动、sandbox 容器创建成功
- gVisor 沙箱 boot 在无 KVM 环境挂起 → 任务预算超时
- GitHub Runner 无 KVM、WSL2 containerd shim 配置未正确透传

### A: OCI/gVisor 接管（TestOCITakeover）⚠️
- **接管断言实际通过**（"gVisor → research-network, task SUCCEEDED"）
- 仅因 sudo worker 在 TempDir 写入 root 文件导致 Go 测试框架清理报错
- 已有 `ociArtifactRoot` 修复，合并 PR #47

---

## 3. 本机 WSL2 验证

| 步骤 | 结果 |
|:-----|:-----|
| WSL2 开启嵌套虚拟化 | ✅ /dev/kvm 出现，KVM API v12 |
| Ubuntu 24.04 + containerd + gVisor | ✅ |
| `runsc run --platform=kvm` 跑 alpine 容器 | ✅ HELLO_FROM_GVISOR_KVM |
| `runsc run`（kvm/systrap/ptrace）跑 containerd bundle | ✅ 全部成功 |
| `ctr run --runtime io.containerd.runsc.v1`（containerd shim） | ⚠️ shim 的 `runsc-sandbox boot` 挂起 |

---

## 4. 已合并 PR（#36–#47）

| PR | 修复 |
|:---|:------|
| #36 | Wasm/OCI 测试重写 + gVisor URL |
| #37 | gVisor tarball 格式（runsc + shim 从 tarball 提取） |
| #38 | containerd 与 docker 冲突（不 apt 安装 containerd 包） |
| #39 | OCI namespace（ctr -n agentos）+ 测试正则 |
| #40 | drill agent binding + 镜像 tag + wasmtime CVE 47.0.4 |
| #42 | ctr images ls -q + 失败码诊断 |
| #43 | checkpoint schema 匹配（hello/v1） |
| #44 | OCI 镜像验证（ls -q 而非 filter 参数） |
| #45 | OCI ctr/worker sudo 访问 containerd socket |
| #46 | OCI worker -dev-mode 参数 |
| #47 | artifact 目录 sudo 清理 + gVisor smoke test |

---

## 5. 后续

- 在有 KVM 的 self-hosted runner 上运行 OCI/gVisor 演练
- 修复 containerd shim → runsc 的平台配置透传（用 CRI options 的 ConfigPath）
- gVisor smoke test 已作为固定 CI 门禁步骤