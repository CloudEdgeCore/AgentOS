# 100K Benchmark — 报告

> 仓库：`CloudEdgeCore/AgentOS`  
> 对应文档：《AgentOS_Next_Production_Validation_Plan.md》§4  
> 基线日期：2026-09-03

## 1. 运行环境

```text
Commit SHA:     7ee64ae（PR #53 lease TTL 修复合入后）
Runner:         GitHub Actions ubuntu-latest（nightly-production-evidence / capacity-baseline）
PostgreSQL:     pgvector/pgvector:pg18 容器服务（同 job 内, 127.0.0.1:5432）
NATS:           未部署（本场景不涉及）
测试入口:       TestControlPlanePipelineCapacityBaseline（AGENTOS_CAPACITY_TASKS=100000）
```

## 2. 测试规模

```text
100,000 Tasks
admission controller ×1（batch 50）
scheduler ×1（batch 50, leaseTTL 按任务量缩放: 100ms/task + 30min）
capacity 池 ×1（100,000 millicore / 12.8M MiB / 100K LLM slots）
全管线: enqueue → admit → schedule → complete
```

## 3. 三次运行结果（全部 PASS）

### Run #1 — run 33718157968

```text
总耗时:           1h01m11s
端到端吞吐:        27.2 tasks/s
Lost / Duplicate / Stuck: 0 / 0 / 0

enqueue:    741 tasks/s   p50=101ms     p95=316ms    p99=548ms
admit:       83 tasks/s   p50=15m28s    p95=19m47s   p99=19m58s
schedule:    58 tasks/s   p50=22m07s    p95=28m37s   p99=28m43s
complete:   164 tasks/s   p50=17m54s    p95=27m30s   p99=28m29s
end-to-end:               p50=55m30s    p95=58m32s   p99=58m51s
```

### Run #2 — run 33718193551

```text
总耗时:           1h19m18s
端到端吞吐:        21.0 tasks/s
Lost / Duplicate / Stuck: 0 / 0 / 0

enqueue:    800 tasks/s   p50=122ms     p95=137ms    p99=149ms
admit:       67 tasks/s   p50=19m18s    p95=24m43s   p99=24m51s
schedule:    38 tasks/s   p50=41m11s    p95=43m34s   p99=43m47s
complete:   196 tasks/s   p50=13m28s    p95=39m52s   p99=43m23s
end-to-end:               p50=1h13m59s  p95=1h16m54s p99=1h17m10s
```

### Run #3 — run 33722793409

```text
总耗时:           1h22m35s
端到端吞吐:        20.2 tasks/s
Lost / Duplicate / Stuck: 0 / 0 / 0

enqueue:    961 tasks/s   p50=102ms     p95=117ms    p99=130ms
admit:       64 tasks/s   p50=19m20s    p95=25m49s   p99=25m57s
schedule:    35 tasks/s   p50=43m47s    p95=47m11s   p99=47m21s
complete:   223 tasks/s   p50=14m58s    p95=43m11s   p99=46m58s
end-to-end:               p50=1h17m57s  p95=1h20m34s p99=1h20m48s
```

## 4. 验收

### 正确性 — 全部通过 ✅

```text
Lost Tasks = 0                    ✅ (3/3 runs, exactness 断言全过)
Duplicate Final Execution = 0     ✅
Deadlock = 0                      ✅
Panic = 0                         ✅
Permanent Stuck Task = 0          ✅
```

### 稳定性 — 共享 CI 硬件上不达标 ❌

```text
吞吐差异:  35.1% (27.2 / 21.0 / 20.2 tasks/s)   要求 <= 10% ❌
P95 差异:  37.7% (58m32s / 1h16m54s / 1h20m34s) 要求 <= 15% ❌
```

**根因**：GitHub hosted runner 每次运行分配到不同的共享 VM。三次运行代码、
配置完全相同，但 schedule 阶段耗时 28m47s / 43m50s / 47m24s——近 1.7 倍差距，
是宿主机性能差异（noisy neighbor），不是系统行为差异。
（Run #1 恰好分到较快 runner，#2/#3 较慢且相互接近：21.0 vs 20.2 = 4%。）

## 5. 结论

1. **正确性证据（核心目标）已成立**：100K 任务全管线 3/3 通过，零丢失、零重复、
   零卡死——"在高负载下保持正确"的主张有了 100K 规模的可审计证据。
2. **性能稳定性无法在共享 CI 硬件上认证**：≤10% 的稳定性标准隐含"同一台机器"
   前提。formal 稳定性认证需要固定硬件（建议复用 soak 自托管 runner，
   同机连续跑 3 次后再出正式性能基线）。
3. 修复记录：本基线过程中发现并修复了 scheduler lease TTL 硬编码 30 分钟
   不足以覆盖 100K 管线（77 分钟）的 pre-existing bug（PR #53，
   修复前 nightly 已连挂 9 次）。

## 6. 后续

- [x] 在固定硬件上重跑 3 次，产出正式性能基线数字 —— 100K 见
      [`100k-stability-2026-09-15.md`](100k-stability-2026-09-15.md)（吞吐 spread 2.56%、
      P95 spread 1.78%，两项达标）
- [x] 1M 基线 —— 见 [`1m-2026-09-16.md`](1m-2026-09-16.md)（吞吐 spread 3.45%、
      P95 spread 2.95%，两项达标）。`capacity-baseline-1m` job 仍因无自托管 runner 而 skip，
      该结果在等效固定硬件上带外产出
