# 1M Capacity Baseline — 2026-09-16

> Repository: `CloudEdgeCore/AgentOS`
> Source commit: `45598a9c09782529359f550446a9f7ff554f8d21` (the commit `v1.1.0` points at)
> Run type: single fixed cloud host, three consecutive runs. Not a CI run — the raw logs
> are published next to this file, in [`1m-2026-09-16/`](1m-2026-09-16/).

## 1. Why this document exists

Every other scale claim in this repository stops at 100K. `README.md` recorded the 1M
baseline as **Not produced**: the `capacity-baseline-1m` nightly job is gated behind a
self-hosted runner pool that has never been provisioned, and three local attempts on
2026-09-12 died at 257K / 48K / 475K of 1M before host-side termination. No 1M run had
ever completed on any machine.

`baseline.md` §2 defines 1M as scenario C and §5 requires three consecutive runs on one
environment inside ≤10% throughput / ≤15% P95 spread. This document is that measurement.

It is also the first capacity number in this repository produced on hardware that meets
the §1 recommended specification — 8 vCPU / 32 GB / NVMe, the floor below which
`baseline.md` says results must not be compared across versions.

## 2. Run environment

```text
Source commit   : 45598a9c09782529359f550446a9f7ff554f8d21
Worktree check  : `git status --porcelain` empty at START; driver records
                  dirty=no. The host ran the tree directly from git, not a
                  materialised copy.
Go              : go1.26.6 linux/amd64
Host            : AWS EC2 m7i.2xlarge, us-east-1
                  Ubuntu 26.04 LTS, kernel 7.0.0-1006-aws x86_64
                  Intel(R) Xeon(R) Platinum 8488C
                  8 vCPU (4 physical cores x 2 threads, single socket) — fixed
                  performance, not burstable
                  30.81 GiB RAM, 8 GiB swapfile (/swapfile)
                  300 GB Amazon EBS gp3 (NVMe), 3000 IOPS / 125 MiB/s baseline
                  root and /home on the same volume (/dev/root, 290 GB)
PostgreSQL      : pgvector/pgvector:pg18 container `agentos-cap-pg`
                  -> PostgreSQL 18.6 (Debian 18.6-1.pgdg12+2), pgvector 0.8.6
                  reachable at 127.0.0.1:5432 on the same host
                  shared_buffers=262144 pages (2 GiB), effective_cache_size=524288
                  pages (4 GiB), max_wal_size=8192 MB, min_wal_size=80 MB,
                  checkpoint_timeout=300s, work_mem=4 MB,
                  maintenance_work_mem=65536 kB, max_connections=100,
                  autovacuum=on, full_page_writes=on, synchronous_commit=off,
                  wal_compression=off
                  No CPU or memory limit was set on the container
                  (HostConfig.Memory=0, NanoCpus=0)
NATS            : not involved in this scenario
Network         : PostgreSQL on the same host, loopback RTT well under 1 ms
Concurrent load : none. The host ran only PostgreSQL and the Go test. It was not
                  shared with any other workload.
Test entry      : TestControlPlanePipelineCapacityBaseline
                  (internal/kernel/store/postgres/capacity_integration_test.go)
                  with AGENTOS_CAPACITY_TASKS=1000000
```

The PostgreSQL configuration is deliberately **identical to the 100K stability run** in
[`100k-stability-2026-09-15.md`](100k-stability-2026-09-15.md) — `shared_buffers=2 GiB`,
`synchronous_commit=off`, same `max_wal_size` — so the two scales sit on one comparable
curve rather than on two differently-tuned databases. The 2 GiB `shared_buffers` is not
tuned for a 14 GB working set; it was kept for comparability.

## 3. Procedure

A driver ran the test three times sequentially, back to back, against the same PostgreSQL
instance, without restarting the database or the container between runs:

```bash
export AGENTOS_CAPACITY_TASKS=1000000
export AGENTOS_TEST_DATABASE_URL='postgres://agentos:...@127.0.0.1:5432/agentos?sslmode=disable'
/usr/local/go/bin/go test -tags=integration -count=1 -timeout 2880m \
  -run '^TestControlPlanePipelineCapacityBaseline$' -v \
  ./internal/kernel/store/postgres/
```

Each run builds its own `agentos_capacity` schema and truncates its own kernel tables, so
the runs are independent of one another and cannot touch a business database. Compilation
was warmed with a compile-only pass before run 1 so the compile step is not billed into the
first measured run.

The test asserts correctness only — it does not look at wall-clock time — so a `PASS` is
not by itself evidence of stability; the spreads below are computed from the `CAPACITY
REPORT` lines it prints.

Spread is defined as `max/min - 1`, the same definition used in the 100K stability
document.

### 3.1 Deviation from the planned procedure — recorded, not smoothed over

**The `-timeout` was raised from `1080m` to `2880m` before this batch, and the change was
not justified by the evidence.** Reasoning at the time: the admission phase was observed
stepping at ~62.7 tasks/s at one instant, which extrapolated to ~20 h total; 18 h
(`1080m`) would therefore not fit. That extrapolation was wrong. The admission phase
actually averaged 85 tasks/s across the whole phase — a single instantaneous sample
understated it by ~35% — and the schedule/admit ratio at 1M is 1.6x, not the 2.85x seen at
100K. Real per-run duration turned out to be ~9.2 h, so `1080m` would have been sufficient
with large margin. The higher timeout therefore did not buy anything; its only cost was the
aborted first attempt below.

**A first attempt was aborted 24 minutes in.** Before restarting with the raised timeout,
the driver had been running since `2026-09-16T04:59:25Z` and had reached 54,950 / 1,000,000
tasks in the admission phase when it was killed to apply the timeout change. Its logs are
retained on the host as `abort1-1m-v110-*` but are **not published here** — they contain no
completed run and no certified number. The published batch starts at
`2026-09-16T05:24:03Z`.

## 4. Results — 3/3 runs completed

| Run | Wall clock | End-to-end throughput | e2e p50 | e2e p95 | e2e p99 |
| --- | --- | --- | --- | --- | --- |
| 1 | 9h9m33.531s | 30 tasks/s | 8h50m20.86s | 9h3m55.014s | 9h6m27.873s |
| 2 | 9h25m42.190s | 29 tasks/s | 9h6m9.431s | 9h19m58.048s | 9h22m34.923s |
| 3 | 9h9m42.495s | 30 tasks/s | 8h50m34.759s | 9h3m58.337s | 9h6m35.743s |

Raw `CAPACITY REPORT` output, verbatim:

```text
Run 1 — 1m-v110-run1.log
  tasks=1000000 wall=9h9m33.531s throughput=30 tasks/s
  enqueue  wall=9m27.037s   throughput=1764 tasks/s submit p50=55ms p95=72ms p99=84ms
  admit    wall=3h16m38.375s throughput=85 tasks/s  queue->admit p50=2h8m21.425s p95=3h13m9.038s p99=3h16m19.855s
  schedule wall=5h14m25.131s throughput=53 tasks/s  admit->run p50=4h10m14.997s p95=5h7m49.13s p99=5h13m20.835s
  complete wall=29m2.983s   throughput=574 tasks/s run->done p50=2h31m49.678s p95=5h8m59.236s p99=5h27m29.504s
  end-to-end p50=8h50m20.86s p95=9h3m55.014s p99=9h6m27.873s

Run 2 — 1m-v110-run2.log
  tasks=1000000 wall=9h25m42.19s throughput=29 tasks/s
  enqueue  wall=9m42.423s   throughput=1717 tasks/s submit p50=57ms p95=72ms p99=81ms
  admit    wall=3h15m16.488s throughput=85 tasks/s  queue->admit p50=2h6m20.992s p95=3h12m38.615s p99=3h14m58.018s
  schedule wall=5h31m28.091s throughput=50 tasks/s  admit->run p50=4h24m30.84s p95=5h25m24.557s p99=5h30m24.685s
  complete wall=29m15.182s  throughput=570 tasks/s run->done p50=2h35m26.807s p95=5h26m2.034s p99=5h44m49.373s
  end-to-end p50=9h6m9.431s p95=9h19m58.048s p99=9h22m34.923s

Run 3 — 1m-v110-run3.log
  tasks=1000000 wall=9h9m42.495s throughput=30 tasks/s
  enqueue  wall=9m10.437s   throughput=1817 tasks/s submit p50=54ms p95=67ms p99=75ms
  admit    wall=3h9m49.297s throughput=88 tasks/s  queue->admit p50=2h1m51.763s p95=3h7m5.477s p99=3h9m19.358s
  schedule wall=5h22m0.137s throughput=52 tasks/s  admit->run p50=4h24m33.278s p95=5h17m7.429s p99=5h21m1.351s
  complete wall=28m42.617s  throughput=581 tasks/s run->done p50=2h24m14.08s p95=5h14m49.791s p99=5h34m13.383s
  end-to-end p50=8h50m34.759s p95=9h3m58.337s p99=9h6m35.743s
```

All three runs ended `exit=0` with `--- PASS: TestControlPlanePipelineCapacityBaseline`
and a `PASS` / `ok` line.

Whole batch: `2026-09-16T05:24:03Z` to `2026-09-17T09:10:20Z` — 27h46m17s for three runs.

## 5. Acceptance

### 5.1 Correctness — passed

```text
Lost tasks                   = 0    PASS (3/3 runs, exactness assertions in the test)
Duplicate final execution    = 0    PASS
Permanent stuck tasks        = 0    PASS
Deadlock / panic             = 0    PASS
```

The test's exactness block asserts, per run, that all 1,000,000 tasks reached `SUCCEEDED`,
that `nonTerminal = 0`, that there is exactly one run and one attempt per task, that no
`runtime_leases` row is left unreleased, that there are exactly 1,000,000 `ADMIT` decisions,
that exactly one outbox event exists per task for each of the eight tracked
`aggregate/event` pairs, and that zero `task_controller_claims` linger. All eight
assertions held in all three runs. The pipeline also reported `deferred=0` and
`maxRetry=0` on every single progress line across all three runs — no task was ever backed
off for placement.

### 5.2 Stability — both targets met

```text
End-to-end throughput spread : 3.45%  (30 / 29 / 30 tasks/s)          target <=10%  MET
End-to-end P95 spread        : 2.95%  (32635.0 / 33598.0 / 32638.3 s) target <=15%  MET
```

Supporting spreads, computed from the same raw numbers:

```text
Wall-clock spread            : 2.94%  (32973.531 / 33942.190 / 32982.495 s)
End-to-end throughput spread : 2.94%  (30.3274 / 29.4619 / 30.3191 tasks/s, exact)
End-to-end p50 spread        : 2.98%  (31820.860 / 32769.431 / 31834.759 s)
End-to-end p99 spread        : 2.95%  (32787.873 / 33754.923 / 32795.743 s)
```

### 5.3 Per-phase throughput

```text
schedule :  6.00%  (53 / 50 / 52 tasks/s)
enqueue  :  5.82%  (1764 / 1717 / 1817 tasks/s)
admit    :  3.53%  (85 / 85 / 88 tasks/s)
complete :  1.93%  (574 / 570 / 581 tasks/s)
```

Unlike the 100K stability run — where the `enqueue` sub-phase alone spread 12.8% and had
to be recorded as a miss — every sub-phase here is inside 10%. This batch does not need
that caveat.

### 5.4 Where the time goes, and what the bottleneck is

The end-to-end pipeline is dominated by two phases. At 1M the schedule phase alone is
5h14m–5h31m and admission is 3h10m–3h17m; together they account for ~93% of wall clock.
Completion is comparatively cheap (28m43s–29m15s at 574–581 tasks/s). Per-task end-to-end
latency is ~9 h, which follows directly: with a strictly serial controller draining 1M
tasks it is arithmetically impossible for the last task to finish sooner than the sum of
the phases.

The binding constraint is **single-core PostgreSQL**, not disk and not memory:

```text
PG container CPU     : mean 117.4%, peak 268.2% across 1637 samples
                       — one core saturated for most of the batch, with occasional
                       multi-core bursts
MemAvailable (min)   : 21405 MB of 30.81 GiB — never under memory pressure
Swap used (peak)     : 28 MB of 8 GiB, mean 8.8 MB — effectively untouched
Disk utilisation     : mean 7.79%, peak 98.70%
Disk write throughput: mean 11.42 MiB/s, peak 126.16 MiB/s
Peak connections     : 20 (PostgreSQL max_connections=100)
```

## 6. What this certifies — and what it does not

**Certified:** on one fixed host meeting `baseline.md` §1, at source commit `45598a9`, three
consecutive 1M-task pipeline runs varied by 3.45% in end-to-end throughput and 2.95% in P95
latency, inside the ≤10% / ≤15% targets, with zero lost, duplicated, or stuck tasks. The
previously empty scenario C in `baseline.md` now has a measured entry.

**Not certified by this document:**

- **A production capacity number.** ~30 tasks/s end to end is what one m7i.2xlarge with a
  containerised PostgreSQL on the same host produced under a deliberately untuned 2 GiB
  `shared_buffers` and a single admission controller and scheduler. It is not a throughput
  figure for supported production hardware or for a tuned deployment, and it should not be
  read as an upper bound — the per-phase numbers show the serial controller drain, not a
  hardware ceiling, is what sets the wall clock.
- **Stability on other hardware or at other scales.** One host, one configuration, one
  scale. The 100K stability result in `100k-stability-2026-09-15.md` is a separate
  measurement on a different host and is not a second data point for this one.
- **Absence of scaling degradation between 100K and 1M.** Not measured here. The two runs
  differ in host, OS, and disk as well as in scale, so subtracting them would not isolate
  scale. A same-host 100K → 1M comparison was not run.
- **The 24h / 72h / 7d soaks.** Still unproduced — see `README.md` Current boundaries.
- **Anything about preemption**, which remains deliberately unimplemented and is documented
  as such in the README.

### 6.1 The disk result needs its own caveat

Disc utilisation peaked at **98.70%** with **126.16 MiB/s** of writes, which is exactly the
gp3 baseline of 125 MiB/s. That is a genuine saturation event and it did not occur at 100K,
where disk utilisation on the workstation run stayed near 4.9%.

It is recorded as a **burst, not a sustained bottleneck**: of 1637 samples, only 82 (5.01%)
were at or above 80% utilisation, and only 28 (1.71%) at or above 95%. 94.56% of samples
were below 20%. The pattern is consistent with checkpoint and WAL flush spikes (the run ends
with `max_wal_size` reached, ~8.2–9.6 GB of WAL) rather than continuous back-pressure, and
the fact that throughput stayed inside 3.45% across three runs while the bursts recurred
argues against the disk being the term that sets run-to-run variance. The dominant
constraint remains the saturated PostgreSQL core. But this is an inference from utilisation
samples, not a controlled experiment: **no run was performed with a faster or larger volume,
so the claim "gp3 is not the bottleneck at 1M" is not established here.**

## 7. Reproducing

The driver and sampler used for this batch are published alongside the logs. On a host with
Go 1.26.6, Docker, and a PostgreSQL 18 reachable over loopback:

```bash
# 1. pin the tree and prove it is the commit you think it is
git -C ~/agentos fetch --all
git -C ~/agentos checkout 45598a9c09782529359f550446a9f7ff554f8d21
git -C ~/agentos status --porcelain    # must be empty

# 2. start PostgreSQL with the §2 configuration, then:
mkdir -p ~/bin ~/capacity-runs
cp run-capacity.py db-sampler.sh ~/bin/ && chmod +x ~/bin/*

# 3. drive three runs (~28 h)
python3 ~/bin/run-capacity.py 1000000 3 1m-v110 \
  'postgres://agentos:...@127.0.0.1:5432/agentos?sslmode=disable' 2880m
```

`run-capacity.py` rewrites `<prefix>.status` after every state change and appends to
`<prefix>-driver.log`, so a killed driver still leaves a truthful record of where it
stopped. It also warms the compiler once before run 1 and samples host memory, swap,
container RSS, connection counts and disk utilisation every 60 s into
`<prefix>-samples.log`.

## 8. Published artifacts

Everything below is the unedited output of the three runs.

| File | Size | SHA-256 |
| --- | --- | --- |
| `1m-v110-run1.log` | 654588 B | `03c4ebbc8ea47ebe115e97dcb0faf8b87c1563d78f019c1b7908589cdd5833ba` |
| `1m-v110-run2.log` | 670926 B | `7175ac149a03026e246e1931a85f25d12ec96b6b3acbaf552fd71a47c09e7714` |
| `1m-v110-run3.log` | 654185 B | `56bc017ef46a8fa4deefab6bb9dda67f4f8f0eb4460ffec348fa7e1d03f5db2c` |
| `1m-v110-summary.json` | 2378 B | `b7c3af0aca60ec63d15a51b55467d54db3495a535a74e3c84f7564ccfc5d8a39` |
| `1m-v110-samples.log` | 264198 B | `d7365922692d3485218a53221955b5fe9689684da03f2b028a9aedd89be61025` |
| `1m-v110-dbsamples.run.log` | 163479 B | `c05424a51eb4bf577096052460d44d4bf144b6573351429278dfe9800d4aaa79` |
| `1m-v110-dbsamples.frozen.log` | 285745 B | `70fe4e4e38e273950f3bbad78f547f5fa847bf7c616181cc85431bcc7c4d9ac4` |
| `1m-v110-driver.log` | 1058 B | `216692fcb310aad917ee89e48707c2d78133a367629e6d98ec96970405da1d3e` |
| `1m-v110.status` | 297 B | `c44c177225041ea397a616d93492f1d1238362282962c78c935f012d039bcfd6` |
| `run-capacity.py` | 11920 B | `adfa2690dadd4b213e6d3faf7a068603af6f71962eb6f5c7b9d733f41119a205` |
| `db-sampler.sh` | 1267 B | `4f3c82af42b3133c16b8f506374bbadb187bc5060b24f6f373c5dd68afd54e84` |

`1m-v110-stdout.log` is also on the host but is empty (0 B, SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`); the driver wrote
child output directly to the per-run logs, so nothing was captured there. It is not
published as evidence.

Driver timeline, verbatim:

```text
[2026-09-16T05:24:02] START tasks=1000000 runs=3 commit=45598a9c09782529359f550446a9f7ff554f8d21 dirty=no go version go1.26.6 linux/amd64
[2026-09-16T05:24:02] WARM compile-only pass (not billed into run 1)
[2026-09-16T05:24:03] RUNNING run=1/3 phase=start started=2026-09-16T05:24:03
[2026-09-16T14:33:51] RUNNING run=1/3 done exit=0 elapsed=549.8min throughput=30.0 p95=9h3m55.014s
[2026-09-16T14:33:51] RUNNING run=2/3 phase=start started=2026-09-16T14:33:51
[2026-09-17T00:00:10] RUNNING run=2/3 done exit=0 elapsed=566.3min throughput=29.0 p95=9h19m58.048s
[2026-09-17T00:00:10] RUNNING run=3/3 phase=start started=2026-09-17T00:00:10
[2026-09-17T09:10:20] RUNNING run=3/3 done exit=0 elapsed=550.2min throughput=30.0 p95=9h3m58.337s
[2026-09-17T09:10:20] SUCCESS tasks=1000000 commit=45598a9c09782529359f550446a9f7ff554f8d21 throughput_spread=3.45% p95_spread=2.95% throughputs=[30.0, 29.0, 30.0] p95_seconds=[32635.0, 33598.0, 32638.3] ended=2026-09-17T09:10:20 peak_disk_util_pct=98.7 peak_disk_write_mibps=126.16 peak_pg_conn=20 end_swap_used_mb=28
```

### 8.1 Note on the two database-sampling files

`1m-v110-dbsamples.frozen.log` is the sampler output as frozen when the sampler was stopped
on 2026-09-18, after the batch had already finished. Because the sampler was not tied to the
driver's lifetime, it kept appending idle-database rows for ~21 h past the end of run 3
(2951 lines total, of which 1287 are post-run idle samples).

`1m-v110-dbsamples.run.log` is the same file truncated at `2026-09-17T09:10:20Z` — the
timestamp of the batch's terminal `SUCCESS` line — so that it covers exactly the three
measured runs and nothing else (1664 lines). All database statistics in §5.4 are taken from
this run-scoped file. Both are published so the truncation is auditable rather than silent.
