# 100K Performance-Stability Baseline — 2026-09-15

> Repository: `CloudEdgeCore/AgentOS`
> Source commit: `45598a9c09782529359f550446a9f7ff554f8d21` (tree `88d963dd8e059bb0dabb55d6f2529a87d75dcd06`,
> the commit `v1.1.0` points at)
> Run type: local, single fixed host. Not a CI run — the raw logs are published next to this
> file, in [`100k-stability-2026-09-15/`](100k-stability-2026-09-15/).

## 1. Why this document exists

[`100k.md`](100k.md) records the 100K-scale pipeline as **correct** on 3/3 runs but **not
certifiable for stability**: the targets (end-to-end throughput spread ≤10%, P95 spread ≤15%)
implicitly assume one machine, and the three CI runs were scheduled onto different shared
VMs, producing a 35.1% / 37.7% spread that `100k.md` attributes to host variance rather than
system behaviour. `100k.md` §5 therefore asks for the same three runs back to back on fixed
hardware.

This document is that measurement. One host, one source tree, one binary, three consecutive
runs.

## 2. Run environment

```text
Source commit   : 45598a9c09782529359f550446a9f7ff554f8d21
Worktree check  : byte-identical to that commit — all 514 blobs fetched from the GitHub Git
                  Data API and compared against git's own blob hash
                  (sha1("blob <len>\0" + content)); 0 mismatches
Go              : go1.26.6 windows/amd64
Host            : Windows 11 Pro build 26200 (10.0.26200, x64)
                  Intel Core i7-13700HX, 16 physical cores / 24 logical processors,
                  2.1 GHz base, 15.7 GiB RAM
PostgreSQL      : pgvector/pgvector:pg18 container -> PostgreSQL 18.6 (Debian 18.6-1.pgdg12+2)
                  reachable at 127.0.0.1:15432
                  shared_buffers=262144 pages (2 GiB), max_wal_size=8192 MB, work_mem=4 MB,
                  fsync=on, synchronous_commit=off, max_connections=100
NATS            : not involved in this scenario
Concurrent load : 15 unrelated containers were running throughout (an unrelated application
                  stack, Redis instances, etc.); each sampled <=2.4% CPU. The host was
                  deliberately not quiesced and not dedicated
Test entry      : TestControlPlanePipelineCapacityBaseline
                  (internal/kernel/store/postgres/capacity_integration_test.go)
                  with AGENTOS_CAPACITY_TASKS=100000
```

## 3. Procedure

A driver ran the test three times sequentially, back to back, against the same PostgreSQL
instance, without restarting the database between runs:

```powershell
go test -tags=integration -count=1 -timeout 360m `
        -run '^TestControlPlanePipelineCapacityBaseline$' -v `
        ./internal/kernel/store/postgres/
# env: AGENTOS_CAPACITY_TASKS=100000
#      AGENTOS_TEST_DATABASE_URL=postgres://agentos:...@127.0.0.1:15432/agentos
```

Each run builds its own `agentos_capacity` schema and truncates its own kernel tables, so the
runs are independent of one another and cannot touch a business database. The test asserts
correctness only — it does not look at wall-clock time — so a `PASS` is not by itself evidence
of stability; the spreads below are computed from the `CAPACITY REPORT` lines it prints.

Spread is defined as `max/min - 1`, the same definition that reproduces the 35.1% / 37.7%
figures in `100k.md` from the CI runs (27.2 / 21.0 / 20.2 tasks/s and 3512 / 4614 / 4834 s).

## 4. Results — 3/3 runs completed

| Run | Wall clock | End-to-end throughput | e2e p50 | e2e p95 | e2e p99 |
| --- | --- | --- | --- | --- | --- |
| 1 | 41m47.945s | 40 tasks/s | 33m50.795s | 37m16.620s | 37m32.981s |
| 2 | 41m42.718s | 40 tasks/s | 33m41.582s | 37m30.557s | 37m50.562s |
| 3 | 42m34.865s | 39 tasks/s | 34m32.440s | 37m56.382s | 38m14.709s |

Raw `CAPACITY REPORT` output, verbatim:

```text
Run 1 — capacity-100k-v110-run1.log
  tasks=100000 wall=41m47.945s throughput=40 tasks/s
  enqueue  wall=4m11.954s  throughput=397 tasks/s  submit p50=246ms p95=320ms p99=382ms
  admit    wall=7m11.553s  throughput=232 tasks/s  queue->admit p50=5m51.691s p95=7m7.223s p99=7m11.608s
  schedule wall=18m46.023s throughput=89 tasks/s   admit->run p50=12m51.632s p95=18m13.695s p99=18m39.23s
  complete wall=11m38.374s throughput=143 tasks/s  run->done p50=15m7.388s p95=18m22.444s p99=18m41.989s
  end-to-end p50=33m50.795s p95=37m16.62s p99=37m32.981s

Run 2 — capacity-100k-v110-run2.log
  tasks=100000 wall=41m42.718s throughput=40 tasks/s
  enqueue  wall=3m47.113s  throughput=440 tasks/s  submit p50=220ms p95=285ms p99=335ms
  admit    wall=7m4.395s   throughput=236 tasks/s  queue->admit p50=5m36.937s p95=6m56.898s p99=7m3.049s
  schedule wall=18m12.479s throughput=92 tasks/s   admit->run p50=12m22.896s p95=17m34.541s p99=18m5.199s
  complete wall=12m38.716s throughput=132 tasks/s  run->done p50=15m41.769s p95=17m53.173s p99=18m6.69s
  end-to-end p50=33m41.582s p95=37m30.557s p99=37m50.562s

Run 3 — capacity-100k-v110-run3.log
  tasks=100000 wall=42m34.865s throughput=39 tasks/s
  enqueue  wall=4m16.089s  throughput=390 tasks/s  submit p50=242ms p95=344ms p99=416ms
  admit    wall=7m23.436s  throughput=226 tasks/s  queue->admit p50=6m1.813s p95=7m15.161s p99=7m21.996s
  schedule wall=19m2.747s  throughput=88 tasks/s   admit->run p50=13m34.831s p95=18m28.448s p99=18m55.95s
  complete wall=11m52.559s throughput=140 tasks/s  run->done p50=14m55.265s p95=18m34.93s p99=18m57.055s
  end-to-end p50=34m32.44s p95=37m56.382s p99=38m14.709s
```

## 5. Acceptance

### 5.1 Correctness — passed

```text
Lost tasks                   = 0    PASS (3/3 runs, exactness assertions in the test)
Duplicate final execution    = 0    PASS
Permanent stuck tasks        = 0    PASS
Deadlock / panic             = 0    PASS
```

### 5.2 Stability — both targets met

```text
End-to-end throughput spread : 2.56%  (40 / 40 / 39 tasks/s)   target <=10%   MET
End-to-end P95 spread        : 1.78%  (2236.6 / 2250.6 / 2276.4 s)  target <=15%  MET
End-to-end p50 spread        : 2.52%  (2030.8 / 2021.6 / 2072.4 s)
End-to-end p99 spread        : 1.85%  (2253.0 / 2270.6 / 2294.7 s)
Wall-clock spread            : 2.08%  (2507.9 / 2502.7 / 2554.9 s)
```

### 5.3 Per-phase throughput — one sub-phase does not meet the 10% figure

```text
enqueue  : 12.82%  (397 / 440 / 390 tasks/s)   <-- above 10%
complete :  8.33%  (143 / 132 / 140 tasks/s)
schedule :  4.55%  ( 89 /  92 /  88 tasks/s)
admit    :  4.42%  (232 / 236 / 226 tasks/s)
```

The `enqueue` sub-phase spread is 12.82%, above the 10% figure the targets are expressed in,
even though the end-to-end metric that the target is defined on met it. The `enqueue` phase is
a short burst (3m47s–4m16s out of a ~42 min run) that runs concurrently with the admission
controller, so it absorbs background scheduling jitter from the host; the variance is not
compensated elsewhere in the pipeline — the total wall-clock spread stays at 2.08%. This is
recorded rather than smoothed over: the certified metrics are end-to-end throughput and P95
spread; no claim is made that every sub-phase stays inside 10%.

## 6. What this certifies — and what it does not

**Certified:** on one fixed host and one source commit, three consecutive 100K-task pipeline
runs varied by 2.56% in end-to-end throughput and 1.78% in P95 latency, inside the ≤10% / ≤15%
targets. The stability targets are reachable and the previously observed 35% / 38% spread is
explained by host variance, not by the kernel's behaviour.

**Not certified by this document:**

- **A production capacity number.** ~40 tasks/s end to end is what one developer workstation
  with a containerised PostgreSQL and 15 unrelated containers competing for the same CPU
  produced. It is not a throughput figure for supported hardware, and it is not comparable to
  the Linux CI numbers in `100k.md` (different OS, different PostgreSQL configuration, notably
  `synchronous_commit=off` here).
- **Stability on other hardware or under other load.** The measurement is one host, one
  configuration. The targets assume fixed hardware; this run shows they are met on *this*
  fixed hardware.
- **The 24h / 72h / 7d soaks and the 1M-scale baseline.** Those remain unproduced — see
  `README.md` Feature status and Current boundaries.
- **Anything about preemption**, which remains deliberately unimplemented and is documented as
  such in the README.

## 7. Reproducing

```powershell
# 1. materialise the exact source tree (this host cannot move its git checkout reliably,
#    so blobs are fetched through the GitHub Git Data API and hash-verified)
python tmp/nightly-runs/sync_tree.py 45598a9c09782529359f550446a9f7ff554f8d21 --write

# 2. run the three-run driver
python tmp/nightly-runs/run-100k-v110.py
```

Both scripts live in the gitignored `tmp/nightly-runs/` scratch directory; `sync_tree.py` is
the only non-obvious piece and is reproduced in [`sync_tree.py`](100k-stability-2026-09-15/sync_tree.py)
so the worktree check can be repeated.

## 8. Published artifacts

Everything below is the unedited output of the three runs.

| File | Size | SHA-256 |
| --- | --- | --- |
| `capacity-100k-v110-run1.log` | 35930 B | `1a3014f327249f3ba9b90d8325527c87f82ab36af8fbed82f311e0391737650b` |
| `capacity-100k-v110-run2.log` | 35334 B | `9a4131ff5d2982b11c97eb5225b5686b24fab7ed4ee4d772f2f00543bf7daea7` |
| `capacity-100k-v110-run3.log` | 36763 B | `3c9756a9afc27f654ec55e138eb277666cae5bbb4545d2e1d44a25fb6d49544c` |
| `capacity-100k-v110-summary.json` | 2434 B | `cc9efecbaca575dafbe360400faa9908d7678e9efee910349da562054103e734` |
| `capacity-100k-v110-driver.log` | 853 B | `edffa10edf9b6602cb744dfe8c7a22595a45e4963c649e012190eb76f2c06654` |
| `sync_tree.py` | 3156 B | `5a2d06b42328f4299cdd78a3c267593824da8716ef2b06364d5400ad6f7a187b` |

Driver timeline:

```text
[2026-09-15T20:32:02] START   source_commit=45598a9... runs=3 tasks=100000
[2026-09-15T21:13:54] run=1/3 done exit=0 elapsed=41.9min throughput=40.0 p95=37m16.62s
[2026-09-15T21:55:40] run=2/3 done exit=0 elapsed=41.8min throughput=40.0 p95=37m30.557s
[2026-09-15T22:38:19] run=3/3 done exit=0 elapsed=42.6min throughput=39.0 p95=37m56.382s
[2026-09-15T22:38:19] SUCCESS throughput_spread=2.6% p95_spread=1.8%
```
