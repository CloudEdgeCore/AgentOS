# AgentOS 运行时隔离演练交付报告（A+B 联合）

**交付日期：** 2026-09-02  
**交付基线：** `main`（PR #36–#50）  
**范围：** 真实沙箱隔离演练（A = OCI/gVisor，B = Wasmtime）+ 跨运行时类接管/重放置

---

## 最终结论

**A+B 联合隔离演练已全部通过。**

| 演练 | 测试 | 验收标准 | 结果 |
|:-----|:-----|:---------|:----:|
| B: Wasmtime 隔离 | TestWasmtimeIsolation | 任务在真实 Wasmtime 沙箱内执行 wasm 组件并 SUCCEEDED | ✅ |
| B: Wasmtime 跨类放置 | TestWasmtimeCrossClassPlacement | preferred → cordon → adapter → restored 三种场景均 SUCCEEDED | ✅ |
| A: OCI/gVisor 隔离 | TestOCIIsolation | 任务在真实 gVisor KVM 沙箱内执行容器并 SUCCEEDED | ✅ |
| A: OCI/gVisor 跨类放置 | TestOCICrossClassPlacement | preferred → cordon → adapter → restored 三种场景均 SUCCEEDED | ✅ |

---

## 演练详情

### B 路径：Wasmtime
- **沙箱**：`agentos-runtime-wasm`（Rust Wasmtime 运行时）
- **验证**：WASM conformance component 在 Wasmtime sandbox 内执行，attempt 落在 `research-wasm/wasm-worker-1`
- **跨类**：cordon Wasmtime pool 后任务落到 `research-network`（adapter），恢复后回到 Wasmtime
- **CI 状态**：稳定通过（0.64s + 1.26s）

### A 路径：OCI/gVisor
- **沙箱**：`agentos-runtime-oci`（Go，通过 `runsc --platform=kvm` 驱动 gVisor）
- **验证**：agent-runtime 容器在 gVisor KVM 沙箱内执行并 SUCCEEDED，attempt 落在 `oci/oci-worker-1`
- **跨类**：cordon oci pool 后任务落到 `research-network`（adapter），恢复后回到 oci
- **执行方式**：新增直接 runsc executor（`-runsc-direct`），绕过 containerd shim 的兼容性问题
- **本机验证**：WSL2 中启用 KVM 嵌套虚拟化，direct runsc 模式完整通过

---

## 已合并 PR 清单（#36–#50）

| PR | 内容 |
|:---|:------|
| #36 | Wasm/OCI 测试重写 + gVisor URL 修复 |
| #37 | gVisor 新版 tarball 格式（runsc + shim 从 tarball 提取） |
| #38 | containerd 与 docker 冲突（不 apt 安装 containerd 包） |
| #39 | OCI namespace + wasm 测试正则修正 |
| #40 | drill agent binding + 镜像 tag + wasmtime CVE 47.0.4 |
| #42 | 镜像验证命令 + 失败码诊断 |
| #43 | checkpoint schema 匹配（hello/v1） |
| #44 | OCI 镜像验证（ls -q + grep） |
| #45 | OCI ctr/worker sudo 访问 containerd socket |
| #46 | OCI worker -dev-mode 参数 |
| #47 | artifact 目录 sudo 清理 + gVisor smoke test |
| #48 | Apache-2.0 LICENSE |
| #49 | 直接 runsc executor（绕过 containerd shim） |
| #50 | OCI 跨类放置测试 + WSL2 本地支持 |

---

## 技术要点

### 直接 runsc executor（PR #49）
新增 `-runsc-direct` 和 `-runsc-platform` 参数，让 OCI worker 可以不经过 containerd shim 直接调用 `runsc run`。适用于 shim 有兼容性问题的主机（WSL2、无 KVM runner）。

实现原理：
1. `ctr images mount <image> <mount>` 挂载镜像 rootfs（只读）
2. `cp -a` 复制到可写 bundle 目录（gofer 需要写权限）
3. 写完整 OCI spec（config.json）
4. `runsc run --platform=kvm --bundle=<bundle> <id>` 执行容器

### 跨类放置
两种 runtime 路径（Wasmtime 组件 + OCI 容器）都验证了跨运行时类放置：
- preferred 落入主池
- cordon 后备池接管（adapter pool 上的 hello runtime）
- 恢复后回到主池

### 环境适配
- 启用 WSL2 嵌套虚拟化（`.wslconfig` → `nestedVirtualization=true`）
- 安装 Ubuntu 24.04 + containerd + gVisor + PostgreSQL + pgvector
- `AGENTOS_RUNSC_DIRECT=1` 环境变量切换 direct runsc 模式

---

## 后续建议

1. 在带 KVM 的 self-hosted runner 上触发 `runtime-isolation-drills` workflow，可验证 A 路径在 CI 中通过
2. 进一步优化 direct runsc executor 的 rootfs 复制性能（当前用 `cp -a`，对大镜像较慢）
3. 考虑将 `AGENTOS_RUNSC_DIRECT` 集成到 `runtime-isolation-drills` workflow 中作为 KVM runner 的显式条件