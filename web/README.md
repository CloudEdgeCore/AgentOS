# Web / Dashboard (planned)

Status: **planned** — part of the v0.9 Platform slice, not yet implemented.

Purpose: TypeScript + React dashboard: Task/Run/Attempt views, Trace Viewer,
Policy debugger, approval simulator and Checkpoint inspector.

Reference stack decisions live in the tech baseline
(§1: "Dashboard: TypeScript + React", §20.2: v0.9). The dashboard consumes only
the public `/v1` REST/SSE contract — never internal gRPC.

This directory is reserved; no dashboard code exists yet.
